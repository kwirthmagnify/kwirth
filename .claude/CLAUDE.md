# reglas de coding

  - nunca uses fichero js, el codigo del proyecto está todo en la carpeta src de cada proyecto
  - trata de evitar los tipos 'any' si existen tipos disponible. Si quieres crear un tipo nuevo avisame para que yo lo valide.
