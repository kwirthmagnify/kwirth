# Provider Debug — histórico de métricas de test

> Registro **incremental** de la suite de tests, una fila por **CL9 / tag**. Se **añade** una fila arriba en
> cada cierre (punto 2 de la checklist CL9); **no se sobrescribe** — es un histórico.
>
> **Cómo se obtiene cada dato:**
> - **Harness** = nº de tests que reporta `npm test` (`node --test`).
> - **Cobertura** = `COVERAGE=1 npm test` (Node `--experimental-test-coverage` con sourcemaps a `src/`). ⚠️ Es
>   sobre los módulos que el harness **carga** (back del canal + common), **no** el 100% del código: los
>   componentes React (`.tsx`) los cubre el e2e (no medido numéricamente).
> - **e2e** = nº de spec files (`e2e/tests/*.spec.ts`) y nº de casos `test()`.

| Fecha | Versión / tag | Harness | Cobertura (líneas / ramas / funcs) | e2e (specs / casos) | Notas |
|---|---|---|---|---|---|
| 2026-09-26 | `plugin/provider-debug@0.1.8` | **43** | **97.62% / 91.87% / 90.06%** | 3 / 30 | **El aviso de recorte ahora cuenta las coincidencias que se quedan fuera.** El buscador cuenta sobre el evento COMPLETO, así que tras el tope de 1000 líneas podía anunciar coincidencias que no se veían por ningún lado, y saltar a una de ellas aterrizaba en la tarjeta en vez de en un resaltado: parecía que el buscador mentía. El aviso del pie lo dice ahora explícitamente —*«N matches fall past the cut and cannot be highlighted here»*— y se pone en color de aviso cuando las hay. Contar son dos pasadas lineales y solo se hacen si hay búsqueda **y** recorte. **+1 test** que se gana el caso en vez de suponerlo: saca el objeto entero del portapapeles, busca una clave que solo exista pasada la línea 1000 y busca esa; si el evento no tuviera ninguna, se salta explicando por qué, en vez de dar un verde vacío. |
| 2026-09-26 | `plugin/provider-debug@0.1.7` | **43** | **97.62% / 91.87% / 90.06%** | 3 / 29 | **Tope de 1000 líneas al pintar un evento.** Un evento de `metrics` de este cluster pasa de 1000 líneas —medido por el propio e2e—, y pintarlas todas cuesta tokenizar el JSON entero y montar miles de `<span>` por tarjeta, para que nadie lea la línea 4000 bajando a rueda. Ahora se recorta antes de tokenizar (el coste del corte se paga una vez, no por token) y debajo queda un aviso más pequeño diciendo cuántas líneas hay en total y que **el botón de copiar sigue dando el objeto entero** — que es cierto: copia desde el evento, no desde el texto pintado. **+1 test** que asegura la invariante «nunca más de 1000 líneas pintadas» pase lo que pase, y que si el aviso aparece sus números cuadran con lo pintado; el test no exige que este evento concreto se recorte, porque eso depende del cluster. ⚠️ **Efecto colateral conocido**: el contador de búsqueda cuenta sobre el JSON COMPLETO, así que puede decir 3 coincidencias y verse resaltadas solo las que caen dentro del recorte. Queda en el backlog. 🐛 **Un e2e atado al inventario otra vez**: dos casos exigían `otel`, que ya no está instalado; pasan a `business` —hoy el único vivo sin `getSubscriptionHelp()`— con nota de qué hacer si algún día publica ayuda. Es la misma lección que ya dejó escrita el caso del provider parado: no nombrar extensiones instaladas. |
| 2026-09-20 | `plugin/provider-debug@0.1.4` | **43** | **97.58% / 91.67% / 90.42%** | 3 / 28 | **Un provider mal escrito ya no puede tumbar el core desde este canal.** `addSubscriber()` y `removeSubscriber()` son async y aquí no se esperaban: lo que fallase al otro lado no acababa en una señal de este canal, acababa en un **unhandled rejection**, y el core sale por su propio handler. Pasó de verdad — suscribirse a `trivy` sin payload mataba Kwirth entero (`reportTypes is not iterable`, arreglado en `provider/trivy@0.1.9`). Ahora el alta y la baja van con su `catch`: el alta manda la señal de error a quien está mirando y la baja se anota en el log, y en los dos casos la instancia se cierra igual. Es el último sitio donde vale asumir que el productor está bien escrito: este canal existe precisamente para hurgar en providers ajenos, y ya trataba así su `getSubscriptionHelp()`. **+2 tests** con dos dobles nuevos (`withBrokenSubscribe`/`withBrokenUnsubscribe`); `node --test` falla el fichero si queda un rechazo sin atender, así que cazan la regresión por sí solos. |
| 2026-09-19 | `plugin/provider-debug@0.1.3` | **41** | **97.47% / 91.19% / 89.74%** | 3 / 28 | **Depurar PLUVIDERS y rediseño del diálogo de setup.** Un pluvider —un plugin que además publica su información in-process— se lista, se configura y se depura **igual que un provider**: el canal resuelve por el prefijo `plugin:` contra el registro de pluviders en vez de contra `clusterInfo.providers`, y para quien depura la diferencia es solo un chip. El catálogo los marca con `pluvider: true` y su descripción, y el mensaje de "no disponible" es propio: un pluvider ausente suele ser un plugin que ni está instalado, no un provider que nadie arrancó. **Diálogo de setup rehecho en tres pestañas** (Overview / Form / JSON) dentro de un cuadro titulado con el id del productor —ni la descripción ni los campos son del diálogo, los declara el productor—, con `USE EXAMPLE` en Overview rellenando el payload y saltando a Form; form y JSON editan el MISMO estado, así que no hay dos sitios que sincronizar. 🐛 **Bug anterior al pluvider**: el `Tab` de Form estaba envuelto en un `Tooltip`, así que no era hijo directo de `Tabs` y no recibía su `onChange` — se salía a JSON y no se podía volver. 🐛 **Dos e2e atados al inventario**: exigían `kafka` y `otel` por id y se pusieron rojos el día que kafka dejó de estar instalado; ahora comprueban que la lista llega poblada y con el estado resuelto, sin nombrar extensiones instaladas. El caso "provider parado" se salta cuando el entorno no tiene ninguno (no se puede provocar desde la UI; la lógica la cubre el harness). Añadido soporte `COVERAGE=1` al runner, que no lo tenía. ⚠️ **La medición cambió en este punto**: hasta aquí el informe promediaba un bundle por fichero de test, cada uno con su propia copia del src y su parcela cubierta, así que daba muy por debajo de lo real. Desde ahora COVERAGE=1 genera un entry único (una sola copia del src) y el número es la UNIÓN. El salto respecto a la fila anterior es de medición, no de tests. |
