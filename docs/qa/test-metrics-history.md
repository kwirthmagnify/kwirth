# Kwirth core — histórico de métricas de test

> Registro **incremental** de la suite de tests del **core** (back + front/e2e), una fila por **CL9 / cierre
> de stream**. Se **añade** una fila arriba en cada cierre (punto 2 de la checklist CL9); **no se
> sobrescribe** — es un histórico.
>
> Vive en `docs/qa/`, la misma ruta que usa cada plugin (`<plugin>/docs/qa/test-metrics-history.md`) — junto
> a los dos PNG que se regeneran de él en cada cierre: evolución de la cobertura (`test-metrics-coverage.png`)
> y tamaño de la suite (`test-metrics-tests.png`, tests que existen). Para que el histórico esté siempre en el
> mismo sitio se mire el core o una extensión. Estuvo en `plans/` hasta el **2026-09-09** (por no publicar
> datos de QA en el website; luego se decidió que publicarlos no importa), y en `docs/plan/` hasta el
> **2026-09-13**, cuando se movió a `docs/qa/` (el sitio propio de los artefactos de QA).
>
> **Cómo se obtiene cada dato:**
> - **Harness** = nº de tests que reporta `npm test` en `back/` (`node --test`).
> - **Cobertura** = `COVERAGE=1 npm test` en `back/` (Node `--experimental-test-coverage` con sourcemaps a
>   `src/`). ⚠️ Es sobre los módulos que el harness **carga**: la lógica del back que los tests ejercitan.
>   El front (React) no entra en esta medida — lo cubre el e2e, no medido numéricamente.
> - **e2e** = nº de spec files (`front/e2e/tests/*.spec.ts`) y nº de casos `test()`. No cuenta los specs de
>   plugins privados (`tests/private/`, excluidos por `testIgnore`) ni el trabajo en curso sin commitear.

## Pendiente sobre la propia suite

- **`help-popup` falla en la corrida completa y pasa en solitario.** Contaminación entre tests: algún spec
  anterior deja estado en la aplicación (el DOM de Kwirth es compartido entre pestañas y diálogos). No es
  un fallo del producto, pero enmascara los de verdad — un rojo que aparece según el orden no sirve de
  señal. Hace falta que cada spec deje la aplicación como la encontró.
- **`webhookManager` › *tokens + configs survive a restart* falla A VECES en la corrida completa y pasa
  siempre en solitario** (visto el 2026-09-17, 4 pasadas sueltas en verde). Mismo patrón que el de
  `help-popup`: estado que deja otro test antes. Un rojo que aparece según el orden no sirve de señal,
  y este vigila que la persistencia de webhooks sobreviva a un reinicio — no es un test menor.
- **La suite completa tarda ~15 minutos**, y Playwright no vuelca nada hasta el final, así que no se puede
  seguir el avance. Demasiado para usarla dentro del ciclo de trabajo: hoy se acaba corriendo por grupos.
- ~~**Ningún spec regenera capturas sin querer.**~~ **HECHO 2026-09-07:** `capture-*.spec.ts` queda fuera de
  la corrida por defecto (`testIgnore`), como los privados. Escribían en `docs/_media/guide`, así que cada
  corrida completa modificaba imágenes de la guía sin que nadie lo pidiera; pasó dos veces en un mismo día
  y hubo que revertir. Se piden a mano al actualizar la guía.

| Fecha | Cierre | Harness | Cobertura (líneas / ramas / funcs) | e2e | Notas |
|---|---|---|---|---|---|
| 2026-09-24 | Kwirth arranca sin Kubernetes (S1 de ECS) | **455** ✅ | **81.98 % / 83.29 % / 79.18 %** | 40 specs · 135 casos ⚠️ | **Todo camino de arranque presuponía un cluster alcanzable, así que Kwirth no podía correr donde no hay Kubernetes.** `createRunningInstance()` montaba siempre los clientes de Kubernetes y pedía el uid de `kube-system`; sin cluster eso lanzaba, lo tragaba el `catch` y el arranque terminaba **sin instancia** — con el proceso vivo escuchando y devolviendo `503`, que es el peor modo de fallo porque parece arrancado. En **Fargate** ni se llegaba ahí: no existe `/.dockerenv` (es containerd), así que el entorno salía `undetected` y el proceso se cerraba. Ahora el entorno de ejecución se detecta con `ecs` incluido, **se publica** en `KwirthData` en vez de perderse al acabar el switch, y de él se derivan en **un solo sitio** (`ExecutionEnvironment.ts`) las capacidades que el arranque necesita: si hay API de Kubernetes y dónde se persiste. Los clientes de Kubernetes —y el propio `loadFromDefault()`, que también puede quejarse sin kubeconfig— quedan dentro de una **rama explícita**, no de un `catch` más ancho: un `catch` convierte "aquí no hay cluster, y está bien" y "el cluster no responde, y eso es un problema" en el mismo silencio. `/healthz` deja de estar atado a `inCluster`, porque un balanceador que no obtiene respuesta recicla la tarea en bucle sin decir por qué. **Y se retiró Docker como fuente de recursos**: se comprobó con un `docker run` sobre la imagen publicada que esa vía **no estaba viva** (`clusterType: kubernetes`, `FetchError` contra `localhost:8080`, `Cannot get a running instance`) — nadie producía `EClusterType.DOCKER` y nadie asignaba `clusterInfo.dockerTools`. Fuera el enum (`common` 0.5.56), `DockerTools.ts`, `dockerode`, `watchDockerPods()`, 10 ramas entre `index.ts` y `ConfigApi.ts`, 8 usos de `isDocker` en el front —incluido un `if/else` con **las dos ramas idénticas**— y `startDockerStream()` de `log` y `alert`. ⚠️ **El enum es un contrato de RUNTIME**: los plugins no empaquetan `kwirth-common`, lo resuelven contra el global del core, así que quitarle un valor deja en `undefined` a todo `dist` ya construido que lo nombre — lo destapó el usuario viendo `[,kubernetes]` en la lista de sources. Por eso hubo que republicar **12 artefactos**: 9 plugins públicos y 3 privados (con sus docs en lockstep). **+14 tests** sobre la detección, las capacidades y el store, con la comprobación de kubeconfig **inyectada** para que no dependan de la máquina donde se corran. ⚠️ **La corrida e2e completa NO se hizo**: se aplazó por decisión del usuario para agruparla con otros cambios; el recuento de specs y casos es estático. Sí pasan los 4 casos del spec nuevo `execution-environment`, y el QA manual validó log, el selector de recursos y los nueve canales republicados. |
| 2026-09-23 | Actualizar una extensión sin desinstalarla | **441** ✅ | **81.73 % / 83.09 % / 78.87 %** | 31 specs · 143 casos | **Subir de versión obligaba a desinstalar primero, y eso se lleva por delante la configuración.** El `install()` de los managers ya reemplazaba por dentro todo lo que hay que reemplazar —índice, código cacheado, módulo del back recargado con `delete require.cache`—: lo único que lo impedía era un guardián copiado nueve veces que rechazaba cualquier id ya instalada. Ahora el permiso es **explícito** (`upgrade` en el POST) y vive en **un solo sitio**, `tools/ExtensionInstallGuard.ts`, que además impide ir hacia atrás: volver a una versión anterior deja el índice diciendo una cosa y la configuración, que no se toca, pensada para otra. **Y lo instalado pasa a ser EXACTAMENTE lo que trae el paquete.** Varias escrituras eran condicionales (`if (meta.frontStored) write(...)`), y saltarse una no deja la clave vacía: la deja con el contenido de la versión ANTERIOR. Un login que deja de traer fondo seguía pintando el de antes; un sender cuyo front deja de caber en el ConfigMap seguía sirviendo el viejo; un provider que retira su esquema seguía enseñando su formulario. Las seis escrituras condicionales pasan a `write(clave, cabe ? valor : null)`, que **borra**. En el front, la acción nueva entra en `ExtensionManagerDialog` —el genérico— así que sale de una vez en los **once tipos y en las dos vistas**, tarjeta y lista; va **entre configurar y la papelera** y está **siempre visible**, deshabilitada con el motivo, porque un botón que aparece y desaparece mueve la papelera al sitio donde estaba el update de la fila de arriba. **Tres casos que el aviso de reinicio no sabía decir**: `ERestartAction.UPDATE` con su propio texto —la extensión no es que no vaya a funcionar, es que sigue funcionando la de ANTES— y se mira `requiresRestart` en **las dos**, la que se va y la que llega, porque el router de la vieja sigue montado aunque la nueva ya no declare ninguno. Un **pack** es el único que no se actualiza en sitio (instalarlo rechaza también si alguno de sus miembros está puesto) y el botón lo dice en vez de ofrecer algo que va a fallar. **+14 tests** (el guardián suelto, y de punta a punta sobre `LoginManager` y `SenderManager`: que el fondo y el front de la versión anterior desaparecen, y que no se borra de más cuando la nueva sí los trae) **+1 spec e2e con 6 casos**, que no pulsa ningún update —actualizar de verdad cambiaría las extensiones del usuario— y fija en una lista los ocho tooltips posibles del botón. ⚠️ De paso se arregló un defecto del propio e2e: `extension-catalogs` usaba `isVisible({ timeout })`, que **no espera** por mucho timeout que se le pase, así que los últimos de los once diálogos se preguntaban con la máquina cargada y respondían que no había catálogo cuando lo que faltaba era un pintado. |
| 2026-09-23 | 0.6.31 | **427** | **81.02% / 82.93% / 78.60%** | 30 / 84 | **Una extension rota deja de tumbar la aplicacion, y las extensiones que RECIBEN dejan de pelearse con el core.** Cuatro arreglos que salieron de un mismo dia de trabajo con un provider de ingesta y un sender de log: **(1)** `getChannelIconSafe` aceptaba como valido un icono cuyo `type` era un objeto —un plugin con un deep import recibe el BARREL entero, y un objeto es truthy—, asi que la home se quedaba EN BLANCO con un simple 'got: object'; ahora se comprueba que el tipo sea renderizable y los seis sitios que pedian el icono a pelo pasan por ahi. **(2)** `ChannelErrorBoundary`: un canal que revienta al pintarse se lleva SU pestaña y un mensaje con su nombre, no la aplicacion. **(3)** El `bodyParser.json()` global se aplicaba antes que los routers publicos de los providers, asi que una extension de ingesta recibia el cuerpo ya parseado —imposible soportar ndjson— y con el limite de 100 kB, que un recolector de log supera sin esfuerzo; ahora existe **`rawBody`** en `IProvider` con un middleware que pone `express.raw` por delante (el patron que ya usaban los webhooks) y el limite es configurable con **`BODYLIMIT`**. **(4)** Node cierra las conexiones ociosas a los 5 s y quien nos envia las REUTILIZA: se sube a 65 s, configurable con **`KEEPALIVE`**, porque del otro lado ese corte se lee como 'kwirth no esta escuchando'. Y dos que destapo el QA del reenvio: **un sender recargado en dev perdia sus configuraciones en silencio** (el rebuild tira la instancia y la nueva nacia vacia; la lista seguia mostrandolas porque sale del almacen del core, no de la instancia), y **un booleano apagado no se guardaba**, solo se omitia — asi que una extension cuyo defecto fuera `true` lo volvia a encender y el interruptor se veia apagado mientras se comportaba como encendido. |
| 2026-09-22 | Entrega por LOTES y el origen dentro del mensaje (S1 del reenvío de log) | **426** ✅ | 80.44 % / 82.96 % / 78.41 % | 37 specs · 121 casos | Primer stream de la cadena Fluent Bit → kwirth → destino. `send` entrega **un mensaje por llamada** con su propio `await`: perfecto para un aviso, inviable para un caudal de log —un round trip por línea— cuando los destinos que se comen volumen aceptan arrays y **cobran por petición**. Se añade `sendBatch?()` **opcional** en `ISenderAccess` (`kwirth-common@0.5.54`) y en `ISender` (`kwirth-common-back@0.5.48`), con un `await` por **lote**, y `SenderManager.sendBatch()` lo reparte: una llamada a quien lo implemente, **entrega de uno en uno a quien no** — esa compatibilidad es lo que hace que el contrato pueda ser opcional sin romper a ningún sender existente. Y el **origen viaja en el mensaje**: `ISenderMessageOrigin` (cluster, namespace, pod, container, source, service, timestamp) como **campo propio**, no dentro de `metadata`, que es JSON libre — un acuerdo tácito ahí se rompe en cuanto hay dos senders, y un destino solo puede etiquetar lo que le llega etiquetado. **+8 tests**: el lote llega entero en una llamada, el orden se respeta en el camino de compatibilidad, el origen llega intacto, un fallo **no sube al llamante** (el reenvío no puede tumbar a quien produce el log) y una línea que falla no cancela las siguientes. ℹ️ Sin e2e: es contrato sin consumidor todavía —nadie llama a `sendBatch` hasta el S3— y no tiene UI; su QA fue de no regresión de los senders vivos. ⚠️ Lección de secuencia: se publicaron los contratos y se siguió tocando el core mientras npm propagaba, dejando el back del usuario sin compilar. Publicar, **esperar a poder instalar**, y entonces tocar el core. |
| 2026-09-22 | Una extensión montada desde dev salía DUPLICADA en tres managers | **426** ✅ | 80.44 % / 82.96 % / 78.41 % | 37 specs · 122 casos | **`listInstalled()` concatenaba el índice de instalados con los metadatos de dev sin filtrar los que se pisan.** Una extensión registrada en `kwirth-dev.json` **sustituye** a la instalada con su mismo id, no se suma a ella — y en un entorno de desarrollo, donde lo normal es tenerla instalada Y además montada desde su `dist`, la misma salía **dos veces**. El duplicado viajaba tal cual por `/core/senders`, `/core/providers` y `/core/webhooks` a todos sus consumidores: los gestores del front y cualquier extensión que liste. `plugin`, `theme`, `login`, `homepage` y `aitoolset` ya llevaban el filtro; **sender, provider y webhook se habían quedado atrás**, y ahora usan el mismo patrón. Lo cazó el **e2e del plugin sender-debug**, cuyo desplegable pintaba `console` repetido: el plugin se defendía deduplicando, pero el defecto estaba aquí. **+8 tests** que montan un workspace de dev de verdad (un `kwirth-dev.json` y un `dist` por extensión) y recorren el camino que hace el core al arrancar, en vez de falsear los mapas de dev a mano; se comprobó que **6 de los 8 se ponen rojos sin el arreglo**. **+1 caso e2e** en el gestor de senders, sobre la respuesta del endpoint y **no** sobre las tarjetas: el gestor pinta instalados *y* disponibles del marketplace, así que un sender publicado sale dos veces con toda la razón — contar tarjetas daba un rojo que no decía nada del bug. ⚠️ El harness incluye 8 tests de `senderBatch.test.ts`, trabajo en curso sin commitear ajeno a este cierre. |
| 2026-09-22 | Probar la configuración de una extensión desde el formulario, y campos de varios valores | **404** ✅ | 78.95 % / 83.38 % / 77.41 % | 37 specs · 121 casos | **Un usuario metía credenciales y no sabía si valían hasta que la extensión fallaba en silencio.** Excubitor lo resolvió a mano con su *Test connection* para los conectores cloud; ahora lo hace el core una vez para cualquier provider: el formulario genérico saca un botón **TEST** si el provider expone `/test` en su `configRouter`. La detección **no se declara**, se mira: `ProviderApi` inspecciona las rutas del router —el endpoint es la única fuente que no puede mentir— así que un provider que lo añada mañana hereda el botón sin tocar su `package.json` ni su build. Ya lo tienen dos: `azure` y `sugarless`. ⚠️ El botón manda el **borrador** por POST y **no guarda**: probar lo que tienes escrito es lo útil, pero guardarlo por tu cuenta puede dejar la configuración peor de como estaba si el formulario se cargó a medias (una extensión que solo acepte GET prueba lo guardado, y entonces sí se avisa de los cambios pendientes). **Y campos de varios valores**: `multiselect` en `IConfigFieldDef` (`kwirth-common@0.5.53`), pintado como desplegable múltiple **con checkbox** — sin el check, un multiselect parece de selección única, y así se quedó en la primera pasada. El valor se guarda como una cadena con comas: el contrato no tiene tipo lista y dárselo obligaría a migrar los 64 campos ya publicados. `azure` lo usa para ofrecer las regiones reales de la suscripción. **+5 tests** de la detección (incluido que `/testing` o `/test/algo` no cuentan) y **+1 spec e2e** con 2 casos, que comprueba el botón y los checkbox sin pulsar TEST — probar de verdad sale a la red del proveedor. ℹ️ `kwirth-common@0.5.53` trae además `previousLogLines`, el ajuste del log del contenedor anterior: el tipo ya está, **consumirlo sigue pendiente** (S2 de su plan). |
| 2026-09-21 | La cobertura del core estaba MAL MEDIDA (y dos managers sin un solo test) | **399** ✅ | 78.78 % / 83.18 % / 77.55 % | 34 specs · 109 casos | 🔴 **El 50 % que veníamos apuntando era un artefacto de medición.** El runner del back bundleaba **un fichero por test**, así que cada bundle arrastraba su **propia copia del `src`** y el informe los trataba como ficheros distintos: hacía la **media** de esas copias en vez de la **unión**, contando como no cubierto todo el `src` que ese test concreto no toca. Encima el desglose listaba `tests/.out/*.test.mjs`, no `src/`, que es la pista que lo delató. Con el entry único —el mismo arreglo que ya llevaban montag, agora y provider-debug— la misma suite, **sin un test nuevo**, pasa de 50.92 a **79.23 %**. El salto es de medición, no de trabajo. ⚠️ El número incluye el código de los propios tests dentro del bundle, así que está algo por encima de la cobertura pura de `src/`; es el mismo criterio que usan los plugins, y por eso es comparable con ellos. **Y lo que sí es trabajo**: `ThemeManager` y `HomepageManager` no tenían **ningún** test —42 de los 83 ficheros del `src` siguen sin aparecer en ninguno— y ahora llevan **11**, que fijan lo que se aprendió a base de sustos: las dos formas de tgz (raíz y `package/`) instalan igual, un front que no cabe en el ConfigMap no se guarda ahí y se recupera del origen con caché, y esa caché se invalida al instalar y al desinstalar. De paso se alinearon con los otros cuatro managers: ya borraban al desinstalar, pero **no al instalar**, que es justo el caso de actualizar. ℹ️ El global baja de 79.23 a 78.78 porque esas ~490 líneas **entran ahora en el denominador**: antes no salían en el informe porque ningún test las importaba. Un test propio cazó un error mío: el front 'enorme' era texto periódico, gzip lo dejaba en nada y cabía en el ConfigMap; hay que generarlo incompresible (`randomBytes`). |
| 2026-09-21 | Un fallo de una extensión ya no tumba el core, y la caché de /tmp se invalida | **388** ✅ | 50.92 % / 75.23 % / 50.38 % | 34 specs · 109 casos | **El core se moría por una promesa sin `catch` de una extensión ajena.** `unhandledRejection` se trataba siempre como fatal: `exitAndLog()` se llevaba el pod por delante, con todos sus canales y todos sus usuarios dentro. Bastaba suscribirse al provider `trivy` sin payload desde provider-debug. Ahora el fallo se **atribuye por el stack** —el core carga cada back desde `/tmp/kwirth-<tipo>-<id>-back.js`, así que ahí está su rastro—: si es de una extensión se **aísla**, se deja traza con su nombre y Kwirth sigue sirviendo. La heurística es a propósito **conservadora**: lo que no se puede atribuir se sigue tratando como fallo del core y el proceso sale, porque ahí sí puede haber quedado un estado del que no conviene fiarse. En `tools/FailureOrigin.ts`, **+7 tests** (incluido el caso asimétrico: un stack del core no se atribuye a nadie). **Y la caché de `/tmp`, que era media solución.** `ProviderManager` no cacheaba lo que bajaba del origen, así que un back que no cabe en el ConfigMap se descargaba entero en cada arranque (914 KB en `trivy`) y el provider se quedaba fuera si el registro no respondía justo entonces. Al ir a copiar el patrón de plugin apareció el defecto de fondo: **ninguno de los cuatro invalidaba su caché**. El nombre no lleva la versión —quien la lee al arrancar solo conoce el id—, así que una actualización seguía cargando el js **viejo** mientras el pod siguiera vivo, y `/tmp` sobrevive a reiniciar el proceso. Ahora provider cachea, los cuatro borran al instalar y al desinstalar, y el nombre vive en un solo sitio (`cachedExtensionFile` / `dropCachedExtensionFiles`) para que el borrado y la lectura no puedan divergir. **+3 tests.** ℹ️ Sin casos e2e nuevos: nada de esto es observable desde la UI —un rechazo aislado se ve en el log del core, y la caché vive en el sistema de ficheros del pod—, así que lo cubre el harness. |
| 2026-09-21 | Portabilidad de configuración: llevarse un Kwirth entero a otro | **378** ✅ | 50.78 % / 75.04 % / 50.09 % | 36 specs · 119 casos | **Un fichero que se saca de un Kwirth y se mete en otro, sin que el core entienda lo que transporta.** La restricción que manda sobre el diseño es que **el core no sabe dónde vive la configuración de una extensión**: Excubitor y Agora la guardan en su propio Postgres, mezclada con datos de trabajo —sus reglas y sus salas son configuración; sus findings y sus mensajes, no—, y solo el plugin sabe cuál es cuál. Nuevo contrato **`IExtension`** en `common-back` —la primera interfaz común a las once familias, que `IChannel`, `IProvider`, `ISender`, `IWebhook` e `IIdpConnector` extienden—, con `exportConfig`/`importConfig` **opcionales**: una extensión ya publicada sigue funcionando sin tocarla. Eso es lo que permite entregar esto sin republicar de golpe los siete de pago. ⚠️ **Opcional no es *fallback*:** quien no lo implementa se declara y no se exporta nada suyo — si el core copiase por su cuenta las claves que conoce, produciría un fichero que *parece* completo y llega al destino sin la mitad. **El giro del día:** no toda la configuración de una extensión la guarda ella. Un sender no puede listar sus propias configuraciones, un conector de IdP no puede enumerar sus instancias (la relación es 1→N y apunta al revés) y ningún toolset conoce sus concesiones: eso lo guarda el CORE. Así que el core **fabrica el interlocutor** (`CoreManagedConfig` + `combine()`) y lo pone donde iría la instancia — sin tipo nuevo ni cambio de formato, porque `ConfigBundleManager` sigue llamando a los dos mismos métodos sin saber quién responde. Con eso **casi toda extensión exporta algo desde el primer día**, sin que su autor mueva un dedo. ⚠️ **Secretos por esquema:** el core no sabe qué campo de un sender es una contraseña, se lo dice su `getConfigSchema()` (`type: 'password'`); sin credenciales se **vacían**, no se omiten, para que el destino sepa cuáles rellenar — y si una extensión **no publica esquema**, su configuración no sale, antes que arriesgar una contraseña en claro en un fichero que acaba en Descargas. El import hace **solo** cuatro cosas: busca, ignora con aviso si no está, ignora con aviso si no implementa, y entrega; una entrada rota **no detiene** a las demás. El bundle es un JSON legible con `formatVersion` desde el primer día. **UI:** los botones Export/Import que ya existían en Settings → Kwirth pasan a manejar la configuración entera, con la lista **por bloques** (General + uno por tipo de extensión, cada uno con su casilla) y respetando las dos promesas que ese diálogo ya hacía: *lo que ves es lo que te llevas* y *nada se guarda hasta OK*. El formato antiguo (`kwirth-settings`) se sigue aceptando al importar. **+33 tests** (18 del transporte + 15 de la config gestionada por el core, centrados en que no se escape un secreto) y **+1 spec e2e con 9 casos** contra el back real, incluido exportar→importar y comprobar que no cambia nada. Adaptadas: **censor**, **pinocchio** (sus triggers; ni los LLMs del almacén común ni el playground) y **provider/http-pull-push** (conserva las credenciales existentes al importar un fichero sin ellas). Los scaffolders de **provider** y **plugin** generan ya el bloque, con las tres reglas que se olvidan. Cascada: `kwirth-common` **0.5.52**, `kwirth-common-back` **0.5.47**. ℹ️ Hallazgos anotados, no tocados: `IdpManager.loadDevIdpConfigs()` existe y **nadie lo llama**, así que la sección `idpConfigs` de `kwirth-dev.json` no se siembra nunca; y `POST /core/idps` respondió 404 al crear una instancia. |
| 2026-09-20 | Recuperar extensiones que no caben en el ConfigMap, y el log del contenedor anterior | **344** ✅ | 49.79 % / 74.07 % / 47.66 % | 34 specs · 109 casos | **Una extensión grande se instalaba bien y desaparecía al primer reinicio.** El `back.js` que no cabe en el ConfigMap (tope de 800 KB comprimido) no se guarda: se vuelve a bajar del origen **en cada arranque**. Y ahí, al contrario que en `install()`, solo se miraba la raíz del tarball extraído — pero un tgz de `npm publish` mete todo dentro de `package/`. El síntoma era un ENOENT sobre `/tmp/kwirth-provider-<id>-src-*/back.js` y, detrás, `Required provider 'trivy' is not registered`. Lo cantó `trivy`, hoy el **único** de los 40 bundles del repo que pasa del límite (1206 KB; el segundo es `senders/email-resend` con 536 KB). Arreglado en un solo sitio, `readTarballFile()` en `PackageRegistries` —el módulo que ya centralizó la descarga por este mismo motivo—, y usado por los **siete** managers con recuperación: provider, plugin, sender y webhook lo tenían mal; theme y homepage llevaban el fallback copiado a mano y aitoolset otra variante. **+6 tests.** ⚠️ **Y el core se moría sin decir por qué**: `exitAndLog()` imprimía `JSON.stringify(reason)`, que sobre un `Error` da exactamente `{}`. Ahora imprime el stack, también en el ConfigMap `kwirth-secure-log`, que es donde se mira el post-mortem. Se descubrió persiguiendo un `unhandled rejection` del provider `trivy` que tumbaba el core entero (arreglado en `provider/trivy@0.1.9`). **Nuevo: el log del contenedor anterior en el About.** Al arrancar en kubernetes y dentro del cluster, el core lee `lastState.terminated` de su propio contenedor y, si reinició, sus últimas 1000 líneas con `previous: true` (`PREVIOUSLOGLINES` lo cambia). Se guarda **en memoria** —en cada arranque se relee, así que persistirlo daría siempre algo más viejo— y se sirve por `GET /managekwirth/previouslog`, **solo con scope admin**: son trazas internas del core. El About lo enseña con un botón que se queda visible aunque no se pueda usar, con el motivo en el tooltip, y si la salida fue anómala se avisa por notificación **una vez por reinicio** (se recuerda la marca de tiempo de aquella muerte, así que recargar la SPA no repite el aviso). ⚠️ `--previous` solo existe si el contenedor reinició **dentro del mismo pod**: tras un rollout el pod es otro y el kubelet no guarda nada, y la UI distingue ese caso de "reinició pero el log ya no está". **+10 tests** y **+1 spec e2e** (2 casos) que comprueba el camino entero UI → autorización → core. El ajuste en la pantalla de settings queda pendiente: obliga a publicar `kwirth-common`. ℹ️ Los casos e2e se cuentan con `grep "^\s*test("` sobre `front/e2e/tests/*.spec.ts`; el 127 de la fila anterior salió de otro criterio — no se ha borrado ningún spec, hoy hay 34 ficheros frente a 33. |
| 2026-09-19 | Fondo de login en alta calidad: `storeLimit()` y dos imágenes | **328** ✅ | 49.53 % / 74.03 % / 47.32 % | 33 specs · 127 casos | **El almacenamiento dice lo que admite, y el login trae dos fondos.** `IConfigMaps` e `ISecrets` ganan `storeLimit()`: **800 KB** en Kubernetes (dentro del ~1 MiB por objeto de etcd) y **`undefined`** —sin techo— en todo lo que escribe en fichero (desktop, Docker, `KWIRTH_STORE`). Hasta ahora el tope del más estrecho se aplicaba a los cuatro. Con eso, un login puede traer `background.png` (el que cabe en cualquier sitio, y cuyo build falla si se pasa) y `background-hi.png` (opcional, sin tope): al instalar se guarda la buena si cabe, si no la normal, y si no cabe ninguna se instala sin fondo y la página lo dice — el comportamiento de antes. En **dev** el fondo se lee del tgz y nunca pasa por el almacenamiento, así que ahí gana siempre la buena. ⚠️ La elección se hace **al instalar**: mover la instalación a `KWIRTH_STORE` no reescribe lo ya instalado, hay que reinstalar. **+12 tests** (7 de la elección pura + 5 de integración que instalan un login de verdad contra un almacén falso, con tope y sin él) y **+1 e2e** (el fondo se sirve como imagen y la página lo pinta). Nuevo `tools/create-kwirth-login.mjs`: hasta ahora un login se creaba copiando `logins/_template/` a mano. |
| 2026-09-19 | La concesión de un toolset sobrevive a reinstalarlo | **316** ✅ | 49.97 % / 74.27 % / 48.03 % | 26 specs · 98 casos · **12 corridos ✅** (`aitoolsets-manager` 9/9 + `pinocchio-toolsets` 3/3) | Salió en el QA de `source-repos@0.2.0`: la tarjeta del gestor decía *concedido a `agora`* mientras el runtime respondía `SIN CONCEDER [los siete toolsets]` y el bot se quedaba **sin ninguna tool**. Las dos mitades no mentían, miraban sitios distintos: la concesión **persistida** vive en el ConfigMap `kwirth-aitoolsets-grants` —que la instalación no toca— y la **efectiva** en un Map en memoria del registro. `install()` llama a `unregisterToolset()` para poder reemplazar, y ese **se lleva la concesión consigo** (correcto al DESINSTALAR: si no, reinstalar resucitaría permisos que nadie volvió a conceder). Resultado: **actualizar un toolset lo revocaba en silencio**, y en dev pasaba en CADA arranque, porque `loadDevAiToolsets()` lanza un IIFE async que el `index.ts` no espera y termina **después** de `applyGrants()`, borrando lo que este acababa de aplicar. **Fix:** `install()` repara lo que rompe — `restoreGrants()` repone desde lo persistido tras re-registrar, solo lo que ya estaba guardado (instalar sigue sin conceder nada a nadie) y solo si el toolset quedó registrado (un id que no llega a registrarse no debe dejar concesiones huérfanas). Con eso la carrera del arranque deja de importar, sin tocar el orden de `index.ts`. **3 tests nuevos** que fijan la regla entera —instalar no concede · reinstalar conserva · desinstalar revoca, también lo guardado—, verificados en rojo con el fix desactivado (315/1). ⚠️ **Y de paso se arregló `pinocchio-toolsets.spec.ts`, que llevaba rojo y NO por un bug**: la lista del selector sale de `resolveTools(…, 'pinocchio')`, así que sin concesión llega vacía aunque todo funcione; el día que los toolsets se concedieron a `agora` el test se puso rojo culpando al registro (*"¿no hay toolsets instalados?"*), que estaba perfecto. Ahora **el test se concede lo que necesita y lo restaura** (snapshot del mapa de grants por la API `/core/aitoolsets/grants`, tomando prestada del front la autorización de la primera petición que sale hacia `/core/`), y su mensaje distingue *no instalado* de *no concedido*. Comprobado que el ConfigMap queda exactamente como estaba. De 1,9 min con reintentos a 45 s en verde. |
| 2026-09-17 | S3: las 43 empaquetadas, y pinocchio resolviendo por el registro | **256** ✅ | 48.58 % / 74.17 % / 47.01 % | 17 specs · 65 casos · **64 ✅, 1 saltado** | `common-ai@0.5.56/57/58`. Las 43 tools dejan de vivir compiladas dentro del core y pasan a **ocho paquetes** `aitoolset` (98 casos de harness propios, ver `aitoolsets/docs/qa/`). **Pinocchio es el primer cliente**: `toolInfoList`, el catalogo `tools` y los cuatro lambdas de traza mueren en su back, y en su sitio quedan `buildAgentTools` + el gancho `observe`. Se nota en la UI: su selector pasa de ofrecer 43 tools —todas, siempre— a ofrecer **solo las de los toolsets instalados**, y eso es lo que fija el e2e nuevo: `describe_pod` si, `delete_pod` (de `k8s-ops`, no instalado) y `times_two` (de `playground`) no. `defineTool` se añade al contrato para que cada tool infiera sus argumentos DESDE su esquema: sin eso, 30 tools portadas serian 30 sitios con `String(args.namespace)` donde el compilador no ayuda. ⚠️ **Las 43 originales NO se han borrado y quedan CONGELADAS** hasta que el resto de plugins esten cableados — un arreglo va solo al paquete. ⚠️ **Gotcha caro**: un toolset construido contra un `common-ai` mas nuevo que el que SIRVE el core no carga (`defineTool is not a function`), y el sintoma es una tarjeta instalada sin contador de tools. ⚠️ `aitoolsets-manager.spec.ts` se puso rojo por un defecto MIO: daba por hecho que el toolset instalado era `playground`. Reescrito para no nombrar ninguno: comprueba el comportamiento del gestor con lo que haya. |
| 2026-09-17 | S2: la cadena de resolución, con precedencia entre toolsets | **256** ✅ | 48.58 % / 74.17 % / 47.01 % | 16 specs · 62 casos · **61 ✅, 1 saltado** | `common-ai@0.5.55`. De *"estos toolsets, en este orden"* a *"estas tools, listas para el LLM"*: `resolveTools` (pura, para que el editor pueda **pintar exactamente lo que se va a ejecutar**) y `buildAgentTools`, el camino único de invocación con sus dos ganchos —**autorizar** antes y **observar** después—, permisivos hasta S4 y S6. **Los choques de nombre se resuelven por PRECEDENCIA, no renombrando** (decisión del usuario): manda el orden de `activeToolsets`, y apagar una tool **no mata el nombre, deja aflorar la del siguiente** — se apaga una referencia (`ts1/td`), no un nombre. El motivo de no cualificar de cara al modelo es duro: los proveedores exigen `^[a-zA-Z0-9_-]{1,64}$`, así que una barra no pasa, y renombrar las 43 cambiaría el comportamiento de agentes cuyos prompts las nombran. 14 casos nuevos fijan la semántica **antes** de que se persista en configuraciones de clientes, incluido el ejemplo literal que planteo el usuario. Un toolset asignado que no esté instalado se **reporta** (`missing`) en vez de ignorarse: si no, el síntoma sería “el agente responde peor” sin pista. `buildAgentTools` envuelve la ejecución en `runWithToolContext`, y eso es lo que permitirá migrar los ocho paquetes de S3 **de uno en uno** en vez de reescribir las 43 antes de poder usar el camino nuevo (hay un test que lo fija con una tool vieja de verdad). ⚠️ **Sin e2e**: S2 no pinta nada — su cliente es pinocchio, y eso es S3. Validado contra los toolsets de S1 y el cluster de dev con `verify.mjs`, que ahora también ejercita precedencia, tapado, denegación y observación. |
| 2026-09-16 | Capabilities: lo que el host le presta a una tool (S1, cierre) | **256** ✅ | 48.58 % / 74.17 % / 47.01 % | 16 specs · 62 casos · **61 ✅, 1 saltado** | `common-ai@0.5.54`. El segundo toolset de validacion, **`k8s-inventory`** (8 tools de inventario, publicado), destapo lo que el primero no podia ver: un `aitoolset` empaquetado **no tenia forma de llegar al cluster**. Las 43 tools de hoy leen un `AsyncLocalStorage` privado de `common-ai` y su accesor **no se exporta** — exportarlo habria repartido el saco entero (`saToken`, `token`, `senders`, `webhooks`, `dockerApi`, ~20 clientes de API) a cualquier paquete de terceros. El contrato pasa a `execute(args, host)`, con `buildToolHost(requires, context)` provisionando **solo lo declarado**: `IK8sCapability` presta 3 clientes de Kubernetes, la identidad del cluster y el mapa de nodos, y nada mas. Los tipos de `@kubernetes/client-node` entran **solo como tipos** (peer opcional), asi que no arrastran el SDK (~6,6 MB) a ningun bundle. Validado: la fachada fue **suficiente** para las 8 tools, y las 8 respondieron contra el cluster de dev (`verify.mjs`). 5 casos nuevos en el harness de `common-ai` (19→24) fijan el reparto, incluida la comprobacion de que la fachada **no deja pasar credenciales**. 13 casos nuevos en el harness propio del toolset. En e2e, el caso que quedo **anotado como pendiente** en el CL9 anterior —la alineacion de columnas de la vista de lista, que con un solo toolset no se puede comprobar— ya es un test de verdad: mide la X de la celda de version de cada fila del catalogo. ⚠️ Las 43 originales **no se han tocado**: los toolsets nuevos llevan copias propias y el camino viejo muere en S3. ⚠️ Sin endpoint de invocacion en el core, a proposito: autorizar al invocar es S4, y abrir un `POST /tools/:name` con solo `validKey` antes de eso dejaria ejecutar tools `write` sin techo. |
| 2026-09-16 | Tipo de extensión `aitoolset` y gestor de extensiones genérico (S1) | **256** ✅ | 48.58 % / 74.17 % / 47.01 % | 16 specs · 61 casos · **60 ✅, 1 saltado** | `common@0.5.49` y `common-ai@0.5.53`. Nace el **11.º tipo de extensión**, `aitoolset`: un paquete que **exporta** un toolset de tools para los LLM y al que el host registra — nadie se auto-registra. Registro único en `common-ai/back` con **ids reservados** para los built-in, `AiToolsetManager` con instalación desde tgz, fichero y **carpeta** (el camino de dev), y `AiToolsetApi` con dos listados que no son lo mismo: `/` es lo **instalado** y `/catalog` lo **registrado y utilizable** — el `toolCount` sale del registro, así que un `back.js` que no cargue se ve sin contador en vez de mentir. **El diálogo NO es a medida**: estrena `ExtensionManagerDialog`, un gestor **genérico dirigido por descriptor** que sustituirá a los diez de siempre (6.093 líneas con solo 22-59 % de duplicación literal: ya habían derivado). Primer artefacto del tipo, `playground` 0.1.0, publicado en npm público con su `aitoolsets/manifest.json`. **Tres invariantes caras las fija el harness**: el id reservado se rechaza **antes** de tocar el índice, un paquete cuyo id no coincide con el del módulo **no se registra** (ni uno ni otro: el índice y el registro dirían cosas distintas), y la instalación desde carpeta **no borra la carpeta** — bug que introduje y que se llevaba por delante el `dist` del usuario en cada arranque. ⚠️ El **reservado estaba detrás del duplicado** y por tanto era código muerto: lo cazó el test nuevo, y se arregló el orden en el **fuente**. En e2e, 8 casos nuevos sobre el genérico (secciones, filtros independientes, vista de lista, veredicto de `canUninstall`, sin engranaje para un tipo sin configuración) y el tipo entra en la regresión de catálogos. El saltado sigue siendo `provider-http-pull-push`. ⚠️ `extension-catalogs` salió **flaky** en la corrida completa (el catálogo de senders pasó de 40 s con la máquina cargada, y ahora recorre seis diálogos): espera subida a 60 s y re-corrido en verde sin reintento. |
| 2026-09-14 | Las credenciales de descarga llegaban tarde al arranque | **246** ✅ | 48.73 % / 74.08 % / 47.08 % | 15 specs · 53 casos · **51 ✅, 1 saltado** | `configurePackageRegistries()` se llamaba en `setUpRoutes()`, pero quien rehidrata las extensiones instaladas es `prepareRunningInstance()`, que corre **antes**. Resultado: toda descarga del arranque salía por el `if (!deps) return {}` de `packageHeaders()`, o sea **anónima**, y contra un registro privado eso es un **401 en cada arranque** — mientras instalar lo mismo desde la UI funcionaba, porque eso pasa después. Detectado en security-pro con las docs de service-flow del Nexus. La llamada se mueve a `createRunningInstance()`, donde se resuelven `configMaps` y `secrets`: un solo sitio, antes de todo, y cubre k8s, docker y desktop. **No era solo docs**: plugins, providers, senders, webhooks, themes y homepages tienen el camino de *fetch from source on startup* para lo que no cupo en el ConfigMap, y también salía anónimo. 2 casos nuevos de harness fijan las dos caras del contrato. ⚠️ **Sin e2e**: es cableado del arranque del back y el e2e observa la SPA ya levantada. Tres asertos de e2e corregidos por obsoletos, no por el cambio: `anonymous` y `jira` ahora salen **dos veces** en su manager (instalado *y* disponible en el marketplace) y rompían strict mode; y dos timeouts cortos (5 s en la página de login de extensión, 15 s en el catálogo) flakeaban con la máquina cargada. |
| 2026-09-09 | Helper `docsUrl` compartido · el login a medias avisa | **232** ✅ | 48.95 % / 73.56 % / 46.69 % | 13 specs · 39 casos · **38 ✅, 1 saltado** | `common-front@0.5.53`. **Muere `DEFAULT_DOCS_URL = 'http://localhost:4000'`** de `HelpButton`: era lo que hacía que una URL de guía mal puesta no hiciera ruido — agora estuvo meses en 404 por un `/core` de menos. Sin base ya **no se pinta el botón**, y al quitarlo salieron **dos diálogos del core** que nunca la pasaban (*Manage login extensions*, *Manage extension packs*) y llevaban desde siempre a ese localhost. `docsUrl(clusterUrl, tipo, id)` en common-front elimina **59 repeticiones** de la misma cadena: excubitor 21, iter 10, core 28. Un **login instalado a medias ahora lo dice**: si `background.png` no cabe en el ConfigMap (~1 MiB duro de k8s) la página muestra un aviso pequeño en rojo que **nombra el login**, sin explicar el motivo — se sirve sin autenticar. 5 casos nuevos fijan la frontera, medida sobre el **base64** y no sobre el PNG. El e2e de `provider-http-pull-push` pasa a **saltarse** si el provider no está cargado: está publicado y en el manifest público, pero un rojo que depende de si alguien lo instaló no es señal. ⚠️ Publicar common-front dejó el front sin compilar hasta **reiniciar el dev**: borrar `node_modules/.cache` no bastó, y `tsc` pasaba porque lee los tipos nuevos mientras webpack seguía con su resolución vieja. |
| 2026-09-09 | Registros de paquetes · procedencia guardada al instalar | **227** ✅ | 49.36 % / 73.38 % / 47.19 % | 13 specs · 39 casos · **38 ✅, 1 ajeno** | `common@0.5.47` y `@0.5.48`. **De dónde se bajan los paquetes deja de colgar del marketplace**: lista propia `packageRegistries`, credencial elegida casando la URL del tarball por prefijo (gana el más largo), Basic **y Bearer** — verificado contra el Nexus real, donde el mismo user token da 200 como Bearer y 401 como Basic. `IMarketplace.auth` eliminado. Un solo `downloadFile` compartido sustituye a **nueve copias** idénticas, ninguna de las cuales mandaba credenciales, y las cabeceras no cruzan a otro host en un redirect. **La procedencia se GUARDA al instalar** (`marketplaceId`/`marketplaceLabel`) en vez de deducirse de la URL: con precedencia por id, dos marketplaces pueden servir el mismo id y había que saber cuál se instaló. `DocsDialog` → `DocsManagerDialog`: el nombre fuera de patrón lo había dejado fuera de **tres** tandas anteriores (Select de versión, procedencia, chips). Nuevos: `packageRegistries` (15 casos) y e2e `package-registries`. Dos aserciones caducadas corregidas, no flaky: `marketplace-secrets` probaba el secreto de paquete que ya no vive ahí, y `help-popup` exigía la página general de senders desde que cada uno enlaza la suya. El rojo restante es `provider-http-pull-push`: el provider no está en `kwirth-dev.json`. |
| 2026-09-07 | Ayuda en los diálogos de configuración · clone de configs de webhook | **198** ✅ | 55.85 % / 73.13 % / 55.10 % | 12 specs · 21 casos · **18 ✅, 1 ajeno, 2 flaky** | `common-front@0.5.52`: `DialogTitleHelp` daba **nombre accesible vacío** al diálogo — MUI reconoce el título por identidad y solo como hijo directo, así que envuelto dejaba el `aria-labelledby` colgando. Afectaba a todos los que ya lo usaban, no solo a los nuevos. El rojo restante es `provider-http-pull-push`: el provider no está en `kwirth-dev.json`, su endpoint da 404. ⚠️ Flaky: `login-extensions: wrong credentials` y `help-popup` del sender — pasan al reintento, y un rojo que depende del reintento enmascara los de verdad. |
| 2026-09-07 | `provider/http-pull-push@0.1.0` · providers dueños de su configuración | **198** ✅ | 55.85 % / 73.07 % / 55.10 % | 12 specs · 24 casos ✅ | Nuevo contrato de core: `configRouter` montado detrás de `validKey` en `/core/providerconfig/<id>`, `IProviderStorage` inyectado en el constructor (`kwirth-store-provider-<id>`, con el flag `secret` decidiendo Secret vs ConfigMap) y `getConfigNames()` para el contador de la tarjeta. `ProviderStorage.ts` + 8 tests suyos entran en el harness del back. El provider nuevo mantiene **su propia suite: 53 tests** (`providers/http-pull-push`), no contados aquí. e2e nuevo `provider-http-pull-push` (403 sin key, split credencial→Secret verificado sobre objetos reales de k8s, diálogo propio, New/Clone/Delete, `Update` persistiendo solo, y el checkbox de credenciales del export). Dos arreglos de UI salidos del propio e2e: el nombre de la conexión pasa a `Connection name` (había dos campos `Name` en el mismo diálogo) y cada botón de borrar gana `aria-label` con su conexión. Sin regresión en `extension-catalogs`. |
| 2026-09-07 | Procedencia en las vistas de lista | **198** ✅ | 55.85 % / 73.13 % / 55.10 % | 12 specs · 24 casos ✅ | Segunda tanda del bloque anterior: el chip de marketplace faltaba en **las veinte** listas (11 diálogos × instalados/disponibles × card/lista), no solo en las tarjetas. `capture-managers` captura ahora también la vista de lista, y las 6 imágenes nuevas se referencian en la guía de cada familia. Sin cambios de back. |
| 2026-09-07 | Consistencia de los 11 managers de extensiones | **198** ✅ | 55.85 % / 73.13 % / 55.10 % | 12 specs · 24 casos, **suite completa en verde** | Sin cambios de back: todo front. `help-popup`, que fallaba en la corrida completa y pasaba en solitario, se arregló solo al sacar `tests/private/` de la corrida por defecto — la contaminación venía de los specs de Iter. Nuevo `capture-managers.spec.ts`, que regenera las 6 capturas de los diálogos desactivando temporalmente los marketplaces privados. La suite tarda 7,8 min (antes 15,2 con los privados dentro). |
| 2026-09-07 | GitHub/Azure DevOps · webhooks en packs · captura redactada | **198** ✅ | 55.89 % / 73.13 % / 55.14 % | 11 specs · 23 casos | `common@0.5.45`. Nuevos: cabeceras de manifest por host (7 casos, GitHub verificado contra la API real) y `extensionDeps` (7). Dos tests que asumían una configuración concreta del entorno pasan a comprobar la rama que toque: `loginExt=anonymous` (el rojo aceptado el 2026-09-04, ya diagnosticado — el login está instalado *sin configurar*, y caer al formulario es lo correcto) y la procedencia en `marketplace-resolve`. `testIgnore: '**/private/**'`: los e2e de plugins de pago ya no se cuelan en una corrida del core. ⚠️ La suite completa tarda ~15 min. |
| 2026-09-06 | `login-censor@0.1.3`, `login-magnify@0.1.3`, `pack-censor@0.1.1` | **185** ✅ | 55.56 % / 72.46 % / 54.34 % | 9 specs · 21 casos | Primera fila del histórico del core. Stream de `extensionType`/`targetType` + secretos de marketplace + endurecimiento del login. Nuevos: `bundledExtensions` (9 casos), precedencia de docs por `(targetType, id)` (5), el guard de promesas y el cuelgue de `/login/password` (5). e2e nuevo `marketplace-secrets`; `marketplace-resolve` pasa a ser robusto a que haya marketplaces privados registrados. Se añade soporte de `COVERAGE=1` al runner del back, que no lo tenía. |
