{
  "name": "kwirth-magnify",
  "version": "0.0.0",
  "description": "Kwirth Magnify Desktop (Tauri)",
  "scripts": {
    "tauri": "tauri",
    "fetch-plugins": "node ../scripts/fetch-bundled.mjs ../back/kwirth-bundled.json resources/bundled",
    "prebuild": "node ../scripts/fetch-bundled.mjs ../back/kwirth-bundled.json resources/bundled",
    "build": "tauri build",
    "dev": "tauri dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@yao-pkg/pkg": "^5"
  }
}
